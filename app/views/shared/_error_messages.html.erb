<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      <strong>The form contained <%= pluralize(object.errors.count, "error") %>.</strong>
    </div>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>