<%= provide(:title, 'All users') %>
<h1>All users</h1>
<div class="row">
	<div class="col-lg-6 col-lg-offset-3" >
	<%= will_paginate %>
	<ul class="users">
		<%= render @users %>
	</ul>
	<%= will_paginate %>
	</div>
</div>
